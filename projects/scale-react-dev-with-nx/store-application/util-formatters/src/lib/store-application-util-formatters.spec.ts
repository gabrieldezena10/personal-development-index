import { storeApplicationUtilFormatters } from './store-application-util-formatters';

describe('storeApplicationUtilFormatters', () => {
  it('should work', () => {
    expect(storeApplicationUtilFormatters()).toEqual(
      'store-application-util-formatters'
    );
  });
});
